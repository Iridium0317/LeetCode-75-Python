# 42. Trapping Rain Water

[LeetCode Link](https://leetcode.cn/problems/trapping-rain-water/)

**Problem**
Given `n` non-negative integers representing an elevation map where the width of each bar is `1`, compute how much water it can trap after raining.

**Example**

```text
Input: height = [0,1,0,2,1,0,1,3,2,1,2,1]
Output: 6

Input: height = [4,2,0,3,2,5]
Output: 9
```

**Constraints:**

* n == height.length
* 1 <= n <= 2 * 10^4
* 0 <= height[i] <= 10^5

### Solution

```python3
class Solution:
    def trap(self, height: List[int]) -> int:
        # # 时间复杂度 o（n)
        # # 空间复杂度 o (n)
        # n = len(height)
        # pre_max = [0] * n
        # pre_max[0] = height[0]
        # for i in range (1,n): # 遍历前缀
        #     pre_max[i] = max(pre_max[i-1], height[i])
        
        # suf_max = [0] * n
        # suf_max[-1] = height[-1]
        # for i in range(n-2, -1, -1): # 遍历后缀
        #     suf_max[i] = max(suf_max[i+1],height[i])

        # ans = 0
        # for h, pre, suf in zip(height, pre_max, suf_max):
        #     ans += min(pre, suf) - h
        # return ans

        # 时间复杂度 o（n)
        # 空间复杂度 o (1)
        n = len(height)
        ans = 0
        left = 0
        right = n-1
        pre_max = 0
        suf_max = 0

        while left<= right:
            pre_max = max(pre_max, height[left])
            suf_max = max(suf_max, height[right])
            if pre_max < suf_max:
                ans += pre_max - height[left]
                left += 1
            else:
                ans += suf_max - height[right]
                right -= 1
        
        return ans      
```

### Note

```text
每个位置能接多少水？取决于：
当前位置的水量 = min(左边最高, 右边最高) - 当前高度

```

**Complexity**

* Time Complexity: $O()$
* Space Complexity: $O()$

---
# 125. Valid Palindrome

[LeetCode Link](https://leetcode.cn/problems/valid-palindrome/)

**Problem**
A phrase is a **palindrome** if, after converting all uppercase letters into lowercase letters and removing all non-alphanumeric characters, it reads the same forward and backward. Alphanumeric characters include letters and numbers.

Given a string `s`, return `true` *if it is a palindrome, or* `false` *otherwise*.

**Example**

```text
Input: s = "A man, a plan, a canal: Panama"
Output: true

Input: s = "race a car"
Output: false

Input: s = " "
Output: true
```

**Constraints:**

* 1 <= s.length <= 2 * 10^5
* s consists only of printable ASCII characters.

### Solution

```python3
class Solution:
    def isPalindrome(self, s: str) -> bool:
        cleaned = [c.lower() for c in s if c.isalnum()]
        left = 0
        right = len(cleaned) -1 
        while left <= right:
            if cleaned[left] != cleaned[right]:
                return False
            left +=1
            right -=1
        
        return True
      
```

### Note

```text
```

**Complexity**

* Time Complexity: $O()$
* Space Complexity: $O()$

---
# 2105. Watering Plants II

[LeetCode Link](https://leetcode.cn/problems/watering-plants-ii/)

**Problem**
Alice and Bob want to water `n` plants in their garden. The plants are arranged in a row and are labeled from `0` to `n - 1` from left to right where the `i`-th plant needs `plants[i]` amount of water.

Alice and Bob each have a watering can. Initially, Alice starts at the **leftmost** plant and Bob starts at the **rightmost** plant. Each watering can has a maximum capacity: Alice's can holds `capacityA` and Bob's can holds `capacityB`.

They water the plants in the following way:

* Alice waters plants from **left to right**, starting from plant `0`.
* Bob waters plants from **right to left**, starting from plant `n - 1`.
* They water plants **simultaneously**.
* If the remaining water in the can is **less than** the amount needed for the current plant, they must **refill** the watering can.
* When Alice and Bob reach the **same plant**, the one with **more water remaining** in their watering can waters the plant. If both have the **same amount**, Alice waters it.

Return the **number of times they have to refill** to water all the plants.

**Example**

```text
Input: plants = [2,2,3,3], capacityA = 5, capacityB = 5
Output: 1

Input: plants = [2,2,3,3], capacityA = 3, capacityB = 4
Output: 2

Input: plants = [5], capacityA = 10, capacityB = 8
Output: 0
```

**Constraints:**

* 1 <= plants.length <= 10^5
* 1 <= plants[i] <= 10^6
* max(plants[i]) <= capacityA, capacityB <= 10^9

### Solution

```python3
class Solution:
    def minimumRefill(self, plants: List[int], capacityA: int, capacityB: int) -> int:
        left = 0
        right = len(plants) - 1
        currentA = capacityA
        currentB = capacityB
        count = 0

        while left < right:
            if plants[left] > currentA:
                count += 1
                currentA = capacityA
            currentA -= plants[left]
            left +=1

            if plants[right] > currentB:
                currentB = capacityB
                count += 1
            currentB -= plants[right]
            right -=1
        # 如果 Alice 和 Bob 到达同一株植物，那么当前水罐中水更多的人会给这株植物浇水
        if left == right and max(currentA, currentB) < plants[left]:
                count += 1
        
        return count      
```

### Note

```text

follow-up:
1. 最后一颗植物的处理： 
    - max(currentA, current B )
    - if currrentA = current B, Alice浇水。
但是对于计算number of times都一样，所以我们可以把这两点要求简化为 max(currentA, currentB) < plants[left]

2. 写一个装水的函数 把重复的"检查水量→refill→浇水"逻辑抽出来
```python3
class Solution:
    def minimumRefill(self, plants: List[int], capacityA: int, capacityB: int) -> int:
        left = 0
        right = len(plants) - 1
        currentA = capacityA
        currentB = capacityB
        count = 0

        def water(current, capacity, plant):
            nonlocal count
            if current < plant:
                count += 1
                current = capacity
            current -= plant
            return current

        while left < right:
            currentA = water(currentA, capacityA, plants[left])
            left += 1
            currentB = water(currentB, capacityB, plants[right])
            right -= 1

        if left == right and max(currentA, currentB) < plants[left]:
                count += 1

        return count
```
```

**Complexity**

* Time Complexity: $O(n)$
* Space Complexity: $O(1)$

---

# 209. Minimum Size Subarray Sum
[LeetCode Link](https://leetcode.cn/problems/minimum-size-subarray-sum/)

**Problem**  
Given an array of positive integers `nums` and a positive integer `target`, return the **minimal length** of a **subarray** whose sum is **greater than or equal to** `target`. If there is no such subarray, return `0` instead.

**Example**

```text
Input: target = 7, nums = [2,3,1,2,4,3]
Output: 2

Input: target = 4, nums = [1,4,4]
Output: 1

Input: target = 11, nums = [1,1,1,1,1,1,1,1]
Output: 0
````

**Constraints:**

* 1 <= target <= 10^9
* 1 <= nums.length <= 10^5
* 1 <= nums[i] <= 10^4

### Solution

```python3
class Solution:
    def minSubArrayLen(self, target: int, nums: List[int]) -> int:
        # 时间复杂度 O(n)
        # 空间复杂度 O(1)
        n = len(nums)
        ans = n+1
        current_sum = 0
        left = 0
        for right,num in enumerate(nums):
            current_sum += num
            while current_sum >= target:
                ans = min(ans, right - left + 1)
                current_sum -= nums[left]
                left += 1
        
        return ans if ans <= n else 0     
```

### Note

```text
思路：
1. 右指针扩张，累加 current_sum。
2. 一旦满足条件就左指针收缩找最小长度。
```
---

# 713. Subarray Product Less Than K
[LeetCode Link](https://leetcode.cn/problems/subarray-product-less-than-k/)

**Problem**  
Given an array of positive integers `nums` and an integer `k`, return the **number of contiguous subarrays** where the **product of all the elements in the subarray is strictly less than `k`**.

**Example**

```text
Input: nums = [10,5,2,6], k = 100
Output: 8
Explanation: The 8 subarrays that have product less than 100 are:
[10], [5], [2], [6], [10,5], [5,2], [2,6], [5,2,6]
````

```text
Input: nums = [1,2,3], k = 0
Output: 0
```

**Constraints:**

* 1 <= nums.length <= 3 * 10^4
* 1 <= nums[i] <= 1000
* 0 <= k <= 10^6

### Solution

```python3 
class Solution:
    def numSubarrayProductLessThanK(self, nums: List[int], k: int) -> int:
        # 时间复杂度 O(n)
        # 空间复杂度 O(1)
        if k <= 1:
            return 0
        
        ans = 0
        prod = 1
        left = 0
        for right, num in enumerate(nums):
            prod *= num
            while prod >= k:
                prod //= nums[left]
                left += 1
                
            ans += right - left + 1 # 每一轮都在累加

        return ans      
```

### Note

```text 
ans += right - left + 1

找每一个 right 窗口，计算对于每个 right 符合的 target。
枚举子数组右端点 right，如果发现子数组不满足要求，就缩小窗口，也就是增大左端点 left。
内层循环结束后，[left,right] 这个子数组是满足题目要求的。由于子数组越短，越能满足题目要求，所以除了 [left,right]，还有 [left+1,right],[left+2,right],…,[right,right] 都是满足要求的。也就是说，当右端点固定在 right 时，左端点在 left,left+1,left+2,…,right 的所有子数组都是满足要求的，这一共有 right−left+1 个，加到答案中。

作者：灵茶山艾府
链接：https://leetcode.cn/problems/subarray-product-less-than-k/solutions/1959538/xia-biao-zong-suan-cuo-qing-kan-zhe-by-e-jebq/

```


---
# 3. Longest Substring Without Repeating Characters
[LeetCode Link](https://leetcode.cn/problems/longest-substring-without-repeating-characters/)

**Problem**  
Given a string `s`, find the length of the **longest substring** without repeating characters.

**Example**

```text
Input: s = "abcabcbb"
Output: 3
Explanation: The answer is "abc", with the length of 3.

Input: s = "bbbbb"
Output: 1
Explanation: The answer is "b", with the length of 1.

Input: s = "pwwkew"
Output: 3
Explanation: The answer is "wke", with the length of 3.
````

**Constraints:**

* 0 <= s.length <= 5 * 10^4
* `s` consists of English letters, digits, symbols and spaces.

### Solution

```python3
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        # left = 0
        # ans = 0
        # window = set()
        # for right, item in enumerate(s):
        #     if item not in window:
        #         window.add(item)
        #     else:
        #         while item in window:
        #             window.remove(s[left])
        #             left += 1
        #         window.add(item)
        #     ans = max(ans, right - left + 1)

        # for right, item in enumerate(s):
        #     while item in window:
        #         window.remove(s[left])
        #         left += 1
        #     window.add(item)
        #     ans = max(ans, right - left + 1)

        ans = left = 0
        cnt = Counter() #不用初始化
        for right, c in enumerate(s):
            cnt[c] += 1
            while cnt[c] > 1: 
                cnt[s[left]] -= 1  
                left += 1  
            ans = max(ans, right - left + 1)  

        return ans
```

### Note

```text
```

**Complexity**

* Time Complexity: $O(n)$
* Space Complexity: $O(min(n, m))$ 其中 m 是字符集大小（比如英文字母就是 26，ASCII 是 128）

---



